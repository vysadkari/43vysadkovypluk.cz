---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class="h-px bg-gradient-to-r from-transparent via-black/50 to-transparent"></div>
<h2 class="bg-gradient-to-r from-transparent via-primary-dark to-transparent p-2 text-center text-3xl font-semibold text-white text-shadow">{title}</h2>
<div class="h-px bg-gradient-to-r from-transparent via-black/50 to-transparent"></div>
